package Chap13;
/*package Chp13;
import java.text.*;
import java.util.*;
interface Enclosure{
	public double getPerimeter();
	public double getArea();
}
class Circle1 implements Enclosure{
	double radius;
	public double getPerimeter() {
		return 2*Math.PI*radius;
	}

	public double getArea() {
		return Math.PI*Math.pow(radius,2);
	}
	
}
class Square implements Enclosure{
	double width;
	public double getPerimeter() {
		return 4*width;
	}

	@Override
	public double getArea() {
		return Math.pow(width,2);
	}
	
}

public class Ex4 {

	public static void main(String[] args) {
		
		  
		  Scanner s=new Scanner(System.in);
		  DecimalFormat df=new DecimalFormat("0.00");
		  
		  Circle1 c=new Circle1();
		  System.out.print("Enter Circle's radius:");
		  c.radius=s.nextDouble();
		  
          System.out.print("Circle's Perimeter is "+df.format(c.getPerimeter()));
          System.out.println(" , Area is "+df.format(c.getArea()));
          
          Square sq=new Square();
          System.out.print("Enter Square's width:");
          sq.width=s.nextDouble();
          
          System.out.print("Square's Perimeter is "+df.format(sq.getPerimeter()));
          System.out.print(" , Area is "+df.format(sq.getArea()));
	}

}*/
